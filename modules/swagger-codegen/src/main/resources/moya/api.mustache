{{#operations}}//
// {{classname}}.swift
//
// Generated by swagger-codegen
// https://github.com/ConnectCorp/swagger-codegen

import Foundation
import Moya

enum {{classname}} {
    {{#operation}}
        case {{operationId}}({{#allParams}}{{dataType}}{{#hasMore}}, {{/hasMore}}{{/allParams}})
    {{/operation}}
}

extension {{classname}}: TargetType {

    var baseURL: NSURL { return NSURL(string: "{{basePath}}") }

    var path: String {
        switch self {
        {{#operation}}
        case .{{operationId}}{{#hasParams}}({{#allParams}}let {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}}){{/hasParams}}:
            return "{{path}}"
        {{/operation}}
        }
    }

    var method: Moya.Method {
        switch self {
        {{#operation}}
        case .{{operationId}}:
            return .{{httpMethod}}
        {{/operation}}
        }
    }

    var parameters: [String: AnyObject]? {
        switch self {
        {{#operation}}
            case .{{operationId}}{{#hasParams}}({{#allParams}}let {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}}){{/hasParams}}:
                return {{^queryParams}}{{^formParams}}[:]{{/formParams}}{{#formParams}}{{^secondaryParam}}[{{/secondaryParam}}
                    "{{paramName}}": {{paramName}}{{#hasMore}},{{/hasMore}}{{^hasMore}}
                ]{{/hasMore}}{{/formParams}}{{/queryParams}}{{#queryParams}}{{^secondaryParam}}[{{/secondaryParam}}
                    "{{paramName}}": {{paramName}}{{#hasMore}},{{/hasMore}}{{^hasMore}}
                ]{{/hasMore}}{{/queryParams}}
        {{/operation}}
        }
    }

    var sampleData: NSData {
        return NSData()
    }
}
{{/operations}}
