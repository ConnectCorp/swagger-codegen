{{#operations}}//
// {{classname}}.swift
//
// Generated by swagger-codegen
// https://github.com/ConnectCorp/swagger-codegen

import Foundation
import Moya

enum {{classname}} {
    {{#operation}}
        case {{operationId}}({{#allParams}}{{dataType}}{{#hasMore}}, {{/hasMore}}{{/allParams}})
    {{/operation}}
}

extension {{classname}}: TargetType {

    var path: String {
        switch self {
        {{#operation}}
        case .{{operationId}}({{#allParams}}let {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}})
            return "{{path}}"
        {{/operation}}
        }
    }

    var method: Moya.Method {
        switch self {
        {{#operation}}
        case .{{operationId}}
            return .{{httpMethod}}
        {{/operation}}
        }
    }

    var parameters: [String: AnyObject]? {
        switch self {
        {{#operation}}
        case .{{operationId}}({{#allParams}}let {{paramName}}{{#hasMore}}, {{/hasMore}}{{/allParams}})
            return []
        {{/operation}}
        }
    }
}
{{/operations}}
